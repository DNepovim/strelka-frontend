export enum BlockTemplates {
  Header = "header",
  GroupList = "groupList",
  Gallery = "gallery",
}

export enum InputType {
  Text = "text",
  TextArea = "textArea",
  Number = "number",
  Checkbox = "checkbox",
  Select = "select",
}
