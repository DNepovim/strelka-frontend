export enum BlockTemplates {
  Header = "header",
  GroupList = "groupList",
  GalleryList = "galleryList",
}

export enum InputType {
  Text = "text",
  TextArea = "textArea",
  Number = "number",
  Checkbox = "checkbox",
  Select = "select",
}
