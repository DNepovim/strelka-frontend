export enum BlockTemplates {
  Header = "header",
  GroupList = "groupList",
}

export enum InputType {
  Text = "text",
  TextArea = "textArea",
  Number = "number",
  Checkbox = "checkbox",
  Select = "select",
}
